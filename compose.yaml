name: nexus-discord-bot-dev

services:
  bot:
    build: .
    pull_policy: build
    environment:
      TOKEN: ${TOKEN}
      TZ: ${TZ}
    image: nexus-discord-bot-dev:latest
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
