name: nexus-discord-bot

services:
  bot:
    environment:
      TOKEN: ${TOKEN}
      TZ: ${TZ}
    image: ghcr.io/delemangi/nexus-discord-bot:latest
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
